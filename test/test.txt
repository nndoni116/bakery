"use strict";

$.ajax({
  url: 'news.json',
  dataType: 'json'
})
.done(function(data) {
  // 成功した時の処理
  const recentNews = data.slice(0, 3);
  const $listContainer = $('#news-list'); // jQueryのセレクタ

  recentNews.forEach(item => {
    $listContainer.append(`
      <li>
        <span class="date">${item.date}</span>
        <span class="category">${item.category}</span>
        <a href="${item.url}">${item.title}</a>
      </li>
    `);
  });
})
.fail(function() {
  // 失敗した時の処理
  console.error("データの読み込みに失敗しました");
});


<CSS>

/* --- 基本設定 --- */
body {
  margin: 0;
  padding: 0;
  font-family: "Helvetica Neue", Arial, "Hiragino Kaku Gothic ProN", "Hiragino Sans", Meiryo, sans-serif;
  color: #333; /* 文字は濃いグレー */
  line-height: 1.8;
  background-color: #f5f5f5; /* 背景は明るいグレー */
}

a {
  text-decoration: none;
  color: inherit;
  transition: 0.3s;
}

/* --- ヘッダー --- */
header {
  background-color: #2c2c2c; /* ヘッダーは深いチャコールグレー */
  color: #fff;
  padding: 15px 0;
  position: sticky;
  top: 0;
  z-index: 100;
}

.header-content {
  max-width: 1000px;
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 20px;
}

.header-logo img {
  height: 50px; /* ロゴを小さく設定 */
  width: auto;
}

h1 {
  font-size: 16px;
  margin: 0;
  font-weight: normal;
  letter-spacing: 0.1em;
}

nav ul {
  display: flex;
  gap: 20px;
  margin: 0;
}

nav a {
  font-size: 13px;
  text-transform: uppercase;
}

/* --- メインコンテンツ --- */
main {
  max-width: 900px;
  margin: 0 auto;
  padding: 60px 20px;
}

section {
  margin-bottom: 120px;
}

h2 {
  font-size: 20px;
  text-align: center;
  margin-bottom: 50px;
  color: #444;
  letter-spacing: 0.2em;
}

/* --- ニュースセクション --- */
#news-list {
  background: #fff;
  border: 1px solid #ddd;
}

#news-list li {
  border-bottom: 1px solid #eee;
  padding: 15px 20px;
  display: flex;
  font-size: 14px;
}

#news-list li:last-child {
  border-bottom: none;
}

#news-list .date {
  width: 100px;
  color: #999;
}

#news-list .category {
  background: #666; /* カテゴリもグレー */
  color: #fff;
  font-size: 10px;
  padding: 2px 8px;
  margin-right: 15px;
  border-radius: 2px;
}

.view-all {
  display: block;
  text-align: center;
  margin-top: 20px;
  font-size: 12px;
  color: #888;
  text-decoration: underline;
}

/* --- Aboutセクション --- */
#about-section {
  text-align: center;
}

.about-description p {
  font-size: 15px;
  color: #555;
  margin-bottom: 40px;
}

.about-button {
  display: inline-block;
  border: 1px solid #444;
  padding: 10px 30px;
  font-size: 13px;
  background: transparent;
}

.about-button:hover {
  background: #444;
  color: #fff;
}

/* --- メニューセクション（画像小さめ設定） --- */
#recent-menu-contents {
  display: grid;
  grid-template-columns: repeat(3, 1fr); /* 3列に並べる */
  gap: 20px;
}

/* もしJavaScriptで画像を出力する場合のスタイル */
.menu-item img {
  width: 100%;    /* 親要素の幅いっぱい */
  max-width: 180px; /* ただし最大180pxまでに制限して小さく見せる */
  height: auto;
  margin-bottom: 10px;
  display: block;
  margin-left: auto;
  margin-right: auto;
  filter: grayscale(20%); /* 少しグレーを混ぜて雰囲気を出す */
}

/* --- スライドショー全体のコンテナ --- */
.main-visual {
  width: 100%;
  height: 60vh; /* 画面の高さの60%に設定（大きすぎず小さすぎず） */
  position: relative;
  overflow: hidden;
  background-color: #2c2c2c; /* 画像読み込み前の背景 */
}

/* --- 各スライドの設定 --- */
.slide {
  width: 100%;
  height: 100%;
  position: absolute;
  top: 0;
  left: 0;
  opacity: 0; /* 最初は透明 */
  transition: opacity 2.5s ease-in-out , transform 12s linear; /* 1.5秒かけてじわっと切り替え */
}

.slide.active {
  opacity: 1; /* activeクラスがついた画像だけ表示 */
  transition: opacity 2.5s ease-in-out, transform 8s linear;
  transform: scale(1.1);
}

.slide img {
  width: 100%;
  height: 100%;
  object-fit: cover; /* 画像の比率を保ったままエリアを埋める */
  filter: grayscale(30%) brightness(70%); /* 少しグレー＆暗くして重厚感を出す */
}

/* --- 重ねる文字 --- */
.main-title {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  color: #fff;
  z-index: 10;
  text-align: center;
}

.main-title p {
  font-size: 24px;
  letter-spacing: 0.3em;
  font-weight: 200;
  border-bottom: 1px solid rgba(255,255,255,0.5);
  padding-bottom: 10px;
}


<slide.js>

(jquery)
$(function() {
  const $slides = $('.slide');
  let currentIndex = 0;

  // 最初の一枚を表示
  $slides.eq(currentIndex).addClass('active');

  // 5秒ごとに切り替える関数
  setInterval(function() {
    const nextIndex = (currentIndex + 1) % $slides.length;
    
    $slides.eq(currentIndex).removeClass('active');
    $slides.eq(nextIndex).addClass('active');
    
    currentIndex = nextIndex;
  }, 5000);
});


(素のjavascript)

document.addEventListener('DOMContentLoaded', () => {
  // 1. 全てのスライド（画像が入った箱）を取得する
  const slides = document.querySelectorAll('.slide');
  let currentIndex = 0; // 今何枚目を表示しているか（0から数える）

  // 2. 最初の一枚に「active」クラスをつけて表示させる
  if (slides.length > 0) {
    slides[currentIndex].classList.add('active');
  }
aaa
  // 3. 5秒ごとに実行するタイマーを作る
  setInterval(() => {
    // 現在表示されているスライドから「active」を消す
    slides[currentIndex].classList.remove('active');

    // 次の番号を計算する（3枚なら 0→1→2→0... とループさせる）
    currentIndex = (currentIndex + 1) % slides.length;

    // 新しい番号のスライドに「active」をつけて表示させる
    slides[currentIndex].classList.add('active');
  }, 7000); // 5000ミリ秒 = 5秒
});

  ＜横並びに変更＞
.news-container {
  display: flex;
  align-items: center; /* 上下の中心を合わせる */
  justify-content: space-between; /* 左右に隙間を空けて配置 */
  gap: 30px; /* 要素同士の間隔 */
}

/* それぞれの幅を調整してバランスをとる */
.news-container h2 {
  flex-shrink: 0; /* タイトルが潰れないように固定 */
  margin-bottom: 0; /* 既存のCSSの余白をリセット */
  white-space: nowrap; /* 改行させない */
}

.news-container #news-list {
  flex-grow: 1; /* リストが真ん中で一番広く広がるようにする */
  margin-bottom: 0; /* 余白リセット */
}

.news-container .view-all {
  flex-shrink: 0; /* リンクが潰れないように固定 */
  margin-top: 0; /* 余白リセット */
  white-space: nowrap;
}

＜news sectionの修正＞
#news-section {
  width: 100%;
  max-width: none; /* もし共通のsection設定で制限がかかっていたら解除 */
  background-color: #e9ecef;
  margin: 0;
  padding: 20px 0; /* 上下に少し余白を。左右は0。 */
}

.news-container {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 30px;
  
  /* ↓ここがポイント！中身のコンテンツだけ最大幅を決めて中央に寄せる */
  max-width: 1200px; /* お好みの幅に */
  margin: 0 auto;    /* 左右の余白を自動にして中央揃え */
  padding: 0 40px;   /* 画面端にピッタリくっつきすぎないよう、内側に余白 */
}

#news-list {
  background: transparent; /* 親の背景色を活かす */
  list-style: none;        /* 黒丸が出ていたら消す */
  padding: 0;
  flex-grow: 1;            /* ニュースを真ん中で広げる */
}

<左寄りに修正＞

.news-container {
    display: flex;
    align-items: center;
    justify-content: space-between; /* 両端に振り分ける */
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 40px;
}

/* 左側グループの設定 */
.news-left-content {
    display: flex;       /* タイトルとリストも横並びにする */
    align-items: center;
    gap: 40px;           /* タイトルとリストの間の距離 */
    flex-grow: 1;        /* 余ったスペースをこのグループが埋める（これでボタンが右に押し出される） */
}

.news-title {
    flex-shrink: 0;      /* タイトルは潰さない */
}

#news-list {
    flex-grow: 1;        /* リスト部分もさらに広がるようにする */
    list-style: none;
    padding: 0;
}

.view-all {
    flex-shrink: 0;      /* ボタンは右端でサイズ固定 */
    white-space: nowrap;
    /* 必要ならマージンで微調整 */
    margin-left: 20px;
}


お知らせタイトル修正

.news-title {
  display: flex;
  flex-direction: column; /* 要素を縦（垂直）に並べる */
  align-items: center;    /* 横方向（左右）の真ん中に寄せる */
  justify-content: center; /* 縦方向の真ん中に寄せる（必要に応じて） */
  flex-shrink: 0;         /* 横並びの中で潰れないように固定 */
}

.news-title h2 {
  margin: 0;              /* 余計な隙間を消す */
  font-size: 18px;
  color: #777;            /* 先ほど相談した薄い色 */
}

.news-title p {
  margin: 0;              /* 余計な隙間を消す */
  font-size: 10px;
  color: #aaa;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}


全体整理

@charset "utf-8";

/* ===============================================
   共通設定
   =============================================== */
*,
::before,
::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  color: #333;
  line-height: 1.8;
  background-color: #f5f5f5;
  /* font-family: "Helvetica Neue", Arial, sans-serif; */
}

a {
  text-decoration: none;
  color: inherit;
  transition: 0.3s;
}

/* ===============================================
   ヘッダー
   =============================================== */
header {
  background-color: #2c2c2c;
  color: #fff;
  padding: 15px 0;
}

.header-content {
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.header-logo img {
  height: 50px;
  width: auto;
}

.header-title {
  font-size: 20px;
  letter-spacing: 0.1em;
}

nav ul {
  list-style: none;
  display: flex;
  gap: 20px;
}

nav a {
  font-size: 13px;
  text-transform: uppercase;
}

/* ===============================================
   メインビジュアル（スライドショー）
   =============================================== */
.main-visual {
  width: 100%;
  height: 60vh;
  position: relative;
  overflow: hidden;
  background-color: #2c2c2c;
}

.main-title {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  color: #fff;
  z-index: 10;
  text-align: center;
}

.main-title p {
  font-size: 24px;
  letter-spacing: 0.3em;
  font-weight: 200;
  border-bottom: 1px solid rgba(255, 255, 255, 0.5);
  padding-bottom: 10px;
}

.slide {
  width: 100%;
  height: 100%;
  position: absolute;
  top: 0;
  left: 0;
  opacity: 0;
  transition: opacity 2.5s ease-in-out, transform 12s linear;
  transform: scale(1);
}

.slide.active {
  opacity: 1;
  transition: opacity 2.5s ease-in-out, transform 8s linear;
  transform: scale(1.1);
}

.slide img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  filter: grayscale(30%) brightness(70%);
}

/* ===============================================
   ニュースセクション
   =============================================== */
#news-section {
  width: 100%;
  background-color: #e9ecef;
  padding: 20px 0;
  margin: 0;
}

.news-container {
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 40px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.news-left-content {
  display: flex;
  align-items: center;
  gap: 40px;
  flex-grow: 1;
}

/* タイトルグループ（縦並び・中央揃え） */
.news-title {
  display: flex;
  flex-direction: column;
  align-items: center;
  flex-shrink: 0;
}

.news-title h2 {
  margin: 0;
  font-size: 18px;
  color: #777;
  font-weight: 500;
  letter-spacing: 0.1em;
  white-space: nowrap;
}

.news-title p {
  margin: 0;
  font-size: 10px;
  color: #aaa;
  letter-spacing: 0.1em;
  text-transform: uppercase;
}

/* ニュースリスト */
#news-list {
  list-style: none;
  flex-grow: 1;
}

#news-list li {
  padding: 15px 20px;
  display: flex;
  font-size: 14px;
}

#news-list .date {
  width: 100px;
  color: #999;
}

#news-list .category {
  background: #666;
  color: #fff;
  font-size: 10px;
  padding: 2px 8px;
  margin-right: 15px;
  border-radius: 2px;
}

/* 一覧を見るリンク（右寄せ） */
.view-all {
  font-size: 12px;
  color: #888;
  text-decoration: underline;
  flex-shrink: 0;
  white-space: nowrap;
  margin-left: 20px;
}

/* ===============================================
   ABOUT / MENU セクション
   =============================================== */
section {
  max-width: 1100px;
  margin: 0 auto 120px auto;
  padding: 0 20px;
}

#about-section {
  text-align: center;
}

.about-description p {
  font-size: 15px;
  color: #555;
  margin-bottom: 40px;
}

.about-button {
  display: inline-block;
  border: 1px solid #444;
  padding: 10px 30px;
  font-size: 13px;
}

.about-button:hover {
  background: #444;
  color: #fff;
}

#recent-menu-contents {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
}

.menu-item img {
  width: 100%;
  max-width: 180px;
  height: auto;
  display: block;
  margin: 0 auto 10px auto;
  filter: grayscale(20%);
}



ドット線

  /* 親要素：ここに含まれる子要素の高さが揃うようになります */
.news-container {
  display: flex;
  align-items: stretch; /* 子要素すべてを一番背の高いものに合わせる */
  /* ...他の設定... */
}

/* ニュースリスト：これが全体の高さを決めます */
#news-list {
  flex-grow: 1;
  margin: 0;
  padding: 10px 0; /* ここで上下に余白を作ると、その分ドット線も伸びます */
}

/* 一覧を見る：ulの高さに合わせて伸び、線を描く */
.view-all {
  align-self: stretch;  /* これで ul と同じ高さになります */
  display: flex;
  align-items: center;  /* 文字は中央に */
  margin: 0 0 0 20px;   /* 余計な上下マージンを消す */
  padding-left: 25px;
  border-left: 2px dotted #fff;
  text-decoration: underline;
  /* heightの指定は削除してください（stretchとぶつかるため） */
}


/* 1. ここを修正：flex-grow: 1 を削除して「左寄せグループ」という性質をなくす */
.news-left-content {
  display: flex;
  align-items: stretch;
  gap: 0;
  /* flex-grow: 1; ←これを削除またはコメントアウト */
}

/* 2. ここを修正：リスト自体が適度に広がるようにする */
#news-list {
  list-style: none;
  flex-grow: 1; /* リストが「タイトル」と「線」の間の余白を埋める */
  max-width: 700px; /* 広がりすぎを防ぐためのストッパー（お好みで調整） */
}

/* 3. ここを修正：コンテナの中身を中央に寄せる */
.news-container {
  max-width: 1050px;
  margin: 0 auto;
  padding: 0 40px;
  display: flex;
  align-items: center;
  justify-content: center; /* space-between から center に変更 */
  gap: 0; /* 隙間は各要素の margin で制御 */
}


/* ===============================================
   メニューセクション（CSSのみで強化）
   =============================================== */

#recent-menu-contents {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: /* ===============================================
   メニューセクション（CSSのみで強化）
   =============================================== */

#recent-menu-contents {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 50px 30px; /* 縦の隙間を広げて、商品間の「品格」を出す */
  margin-top: 40px;
}

.menu-item {
  text-align: center;
  padding: 10px;
  transition: 0.3s;
}

/* 画像のブラッシュアップ */
.menu-item img {
  width: 100%;
  max-width: 200px; /* 少しサイズアップ */
  height: 200px;    /* 高さを固定して揃える */
  object-fit: cover; /* 画像が歪まないように調整 */
  border-radius: 50%; /* パン屋さんらしい、丸いフォルムに（お好みで） */
  margin: 0 auto 20px auto;
  filter: sepia(20%) brightness(90%); /* 少し温かみのある色調に */
  box-shadow: 0 5px 15px rgba(0,0,0,0.05); /* 柔らかな影 */
  transition: 0.4s;
}

/* ホバー時の演出（CSSだけでできる工夫） */
.menu-item:hover img {
  transform: scale(1.05);
  filter: sepia(0%) brightness(100%);
  box-shadow: 0 8px 25px rgba(0,0,0,0.1);
}

/* --- CSSだけで「商品名」の下に飾り線を入れる --- */
.menu-item h3 {
  font-size: 16px;
  color: #555;
  margin-bottom: 15px;
  position: relative;
  display: inline-block; /* 線を文字の幅に合わせる */
}

.menu-item h3::after {
  content: "";
  position: absolute;
  bottom: -8px;
  left: 50%;
  transform: translateX(-50%);
  width: 30px;
  height: 1px;
  background-color: #ccc; /* さりげない区切り線 */
}

/* --- 商品説明（もしHTMLにある場合）の調整 --- */
.menu-item p {
  font-size: 13px;
  color: #888;
  line-height: 1.6;
}; /* 縦の隙間を広げて、商品間の「品格」を出す */
  margin-top: 40px;
}

.menu-item {
  text-align: center;
  padding: 10px;
  transition: 0.3s;
}

/* 画像のブラッシュアップ */
.menu-item img {
  width: 100%;
  max-width: 200px; /* 少しサイズアップ */
  height: 200px;    /* 高さを固定して揃える */
  object-fit: cover; /* 画像が歪まないように調整 */
  border-radius: 50%; /* パン屋さんらしい、丸いフォルムに（お好みで） */
  margin: 0 auto 20px auto;
  filter: sepia(20%) brightness(90%); /* 少し温かみのある色調に */
  box-shadow: 0 5px 15px rgba(0,0,0,0.05); /* 柔らかな影 */
  transition: 0.4s;
}

/* ホバー時の演出（CSSだけでできる工夫） */
.menu-item:hover img {
  transform: scale(1.05);
  filter: sepia(0%) brightness(100%);
  box-shadow: 0 8px 25px rgba(0,0,0,0.1);
}

/* --- CSSだけで「商品名」の下に飾り線を入れる --- */
.menu-item h3 {
  font-size: 16px;
  color: #555;
  margin-bottom: 15px;
  position: relative;
  display: inline-block; /* 線を文字の幅に合わせる */
}

.menu-item h3::after {
  content: "";
  position: absolute;
  bottom: -8px;
  left: 50%;
  transform: translateX(-50%);
  width: 30px;
  height: 1px;
  background-color: #ccc; /* さりげない区切り線 */
}

/* --- 商品説明（もしHTMLにある場合）の調整 --- */
.menu-item p {
  font-size: 13px;
  color: #888;
  line-height: 1.6;
}


*/========ABOUTページ＝＝＝＝＝＝＝＝＝＝/*

<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ABOUT | ハード系のパン屋</title>
    <link rel="stylesheet" href="./css/style.css">
</head>
<body id="about-page">

    <header>
        <div class="header-content">
            <div class="header-logo">
                <a href="./index.html"><img src="./images/bread-logo.png" alt="パン屋ロゴ"></a>
            </div>
            <h1 class="header-title">ハード系のパン屋</h1>
            <nav>
                <ul>
                    <li><a href="./about.html">about</a></li>
                    <li><a href="#news">news</a></li>
                    <li><a href="#menu">menu</a></li>
                    <li><a href="#contact">contact</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main>
        <div class="about-hero">
            <img src="./images/about-main.png" alt="パン作りの風景">
            <div class="about-hero-title">
                <h2>私たちの想い</h2>
                <p>Philosophy</p>
            </div>
        </div>

        <section class="about-concept">
            <div class="inner">
                <h3>「噛むほどに、物語がはじまる。」</h3>
                <p>
                    当店のパン作りは、朝一番の粉の温度を測ることから始まります。<br>
                    使用するのは、私たちが信頼する農家さんから届く国産小麦と、創業時から大切に育ててきた自家製酵母。<br>
                    派手さはありませんが、飽きのこない、嘘のないパン作りを続けています。
                </p>
            </div>
        </section>

        <section class="about-points">
            <div class="inner">
                <div class="point-item">
                    <span class="number">01</span>
                    <h4>自家製酵母</h4>
                    <p>季節ごとに表情を変える自家製酵母。時間をかけてゆっくり育てることで、独特の酸味と香りが生まれます。</p>
                </div>
                <div class="point-item">
                    <span class="number">02</span>
                    <h4>石窯での直火焼き</h4>
                    <p>高温の石窯で一気に焼き上げることで、外側はバリッと、中は水分を閉じ込めたもっちり食感に。</p>
                </div>
                <div class="point-item">
                    <span class="number">03</span>
                    <h4>国産小麦の選定</h4>
                    <p>日本各地の農家から届く小麦を、パンの種類に合わせて配合。素材本来の甘みを引き出します。</p>
                </div>
            </div>
        </section>

        <section id="eat-in-section" class="about-eatin">
            <div class="eat-in-container">
                <div class="eat-in-image">
                    <img src="./images/eatin.png" alt="イートインスペース">
                </div>
                <div class="eat-in-text">
                    <h2>イートインスペース</h2>
                    <p class="eng-title">Eat-in Space</p>
                    <div class="description">
                        <p>焼きたてのパンをその場でお楽しみいただける、<br>開放的なカフェスペースをご用意しております。</p>
                        <p>こだわりの自家焙煎コーヒーとともに、<br>ゆったりとしたひとときをお過ごしください。</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="company-overview">
            <div class="inner">
                <div class="section-title-center">
                    <h2>会社概要</h2>
                    <p>Company Overview</p>
                </div>
                <dl class="company-list">
                    <div class="company-row">
                        <dt>会社名</dt>
                        <dd>株式会社 Gemini Bakery</dd>
                    </div>
                    <div class="company-row">
                        <dt>設立</dt>
                        <dd>2010年4月</dd>
                    </div>
                    <div class="company-row">
                        <dt>代表者</dt>
                        <dd>代表取締役 山田 太郎</dd>
                    </div>
                    <div class="company-row">
                        <dt>事業内容</dt>
                        <dd>製パンの製造・販売、カフェ運営、自家焙煎コーヒーの卸売</dd>
                    </div>
                </dl>
            </div>
        </section>
    </main>

    <footer id="main-footer">
        </footer>

</body>
</html>

あ